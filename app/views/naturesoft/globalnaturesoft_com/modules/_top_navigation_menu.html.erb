<nav class="top-nav">
    <ul>
        <% Naturesoft::Menus::Menu.where(parent_id: nil).order(custom_order: "asc").each do |menu| %>
            <li>
                <% if menu.children.order(custom_order: "asc").empty? %>
                    <a href="<%= menu.url %>">
                        <span><%= menu.name %></span>
                    </a>
                <% else %>
                    <a href="<%= menu.url %>">
                        <span><%= menu.name %></span>
                    </a>
                    <ul>
                        <% menu.children.order(custom_order: "asc").each do |child| %>
                            <li>
                                <a href="<%= child.url %>">
                                    <span><%= child.name %></span>
                                </a>
                            </li>
                        <% end %>
                    </ul>
                <% end %>
            </li>
        <% end %>
    </ul>
</nav>

